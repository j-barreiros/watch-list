import styled from 'styled-components';

const StyledCastSlider = styled.section`
    display: flex;
    gap: 10px;
    overflow-x: scroll;
    padding-bottom: 10px;
`

export default StyledCastSlider;