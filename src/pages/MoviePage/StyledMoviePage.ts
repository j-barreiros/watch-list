import styled from 'styled-components'

const StyledMoviePage = styled.section`

    .infoSection {
        display: flex;

        .movie_poster {
            width: 300px;
            border-radius: 10px;
        }

        .movie_title {
            font-size: 2.2rem;
            font-weight: 700;
        }
    }
`

export default StyledMoviePage;